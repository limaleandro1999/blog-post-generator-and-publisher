{
  "name": "blog-automation-lambda",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "serverless offline start",
    "dynamodb:start": "sls dynamodb start --migrate"
  },
  "dependencies": {
    "aws-sdk": "^2.1367.0",
    "axios": "^1.3.6",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-localstack": "^1.0.6"
  }
}
